[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.coverage.run]
branch = true
source = ["paper_checker"]

[tool.coverage.report]
fail_under = 90
show_missing = true
skip_covered = false

